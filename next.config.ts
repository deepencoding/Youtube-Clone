import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['settling-admittedly-asp.ngrok-free.app', '*.settling-admittedly-asp.ngrok-free.app'],
};

export default nextConfig;
